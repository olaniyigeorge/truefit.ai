[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = . src
asyncio_mode = auto
markers =
    unit: pure domain / service logic, no database
    integration: tests that exercise repository or DB behaviour
    essential: mark test as essential to run before server start